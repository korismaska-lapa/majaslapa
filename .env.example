PORT=5173
NODE_ENV=production
ADMIN_PASSWORD=change-this-password
CONTACT_TO=korismaska@gmail.com,dirigents@gmail.com,laurarozenberga2@gmail.com
MAIL_FROM=Koris MASKA <korismaska@korismaska.lv>
# Optional SMTP (otherwise cPanel sendmail / localhost:25 is used)
# SMTP_HOST=mail.korismaska.lv
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=

# Docker compose bind-mounts ./content, ./public/media/uploads, and
# ./public/media/voices. Leave DATA_DIR unset so those default paths are used.
# Local Docker on this PC can use ADMIN_PASSWORD=123 (do not commit a real .env).
#
# Railway volume only:
# DATA_DIR=/data
